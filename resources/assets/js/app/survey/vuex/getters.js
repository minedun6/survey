export const questions = (state) => {
    return state.questions;
};

export const questionTypes = (state) => {
    return state.questionTypes;
};

export const newChoice = (state) => {
    return state.newChoice;
};